{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd portfolio_backend && python manage.py migrate && gunicorn portfolio_backend.wsgi",
    "healthcheckPath": "/api/"
  }
}