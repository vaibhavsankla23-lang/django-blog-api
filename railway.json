{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd portfolio_backend && python manage.py migrate && python manage.py shell -c \"from django.contrib.auth.models import User; User.objects.create_superuser('vaibh', 'vaibh@gmail.com', 'Vaibh@123') if not User.objects.filter(username='vaibh').exists() else None\" && gunicorn portfolio_backend.wsgi",
    "healthcheckPath": "/api/"
  }
}